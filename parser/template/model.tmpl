package {{.PackageName}}
{{.Imports.Model}}

type {{.Name}} struct {
		{{range .Properties}} {{.Name}} {{.Type}} {{.Tag}}
		{{end}}
}
 
type {{.PluralName}} []*{{.Name}}

func(m {{.PluralName}}) Ids() (ids []{{.Id.Type}}) {
	for _, mm := range m {
		ids = append(ids, mm.{{.Id.Name}})
	}
	return
}